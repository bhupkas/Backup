using namespace std;

#include "bits/stdc++.h"

vector < pair < int , int > > adj[5005];

map < string , int > mp;

int main()
{
        int t;
        cin >> t;
        int n;
        for(int tc = 1 ; tc <= t ; tc++)
        {
                mp.clear();
                cin >> n;
                for(int i = 0 ; i < n ; ++i)    adj.clear();
                
        }
        return 0;
}
