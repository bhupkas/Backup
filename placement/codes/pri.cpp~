using namespace std;

#include "bits/stdc++.h"

struct node
{
        int a;
        int b;
        bool operator < (struct node other) const
        {
                a < other.a;
        }
};
typedef struct node Node;

int main()
{
        priority_queue < Node > pq;
        return 0;
}
