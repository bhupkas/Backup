/* bhupkas */

using namespace std;

#include "bits/stdc++.h"

int LCM(int a, int b)
{
	return  (a * b) / __gcd(a,b);
}

int main()
{
	int n,m,k;
	cin >> n >> m >> k;
	if(LCM(n,k) % m == 0 && LCM(m,k) % n == 0)	cout << "YES" << endl;
	else	cout << "NO" << endl;
	return 0;
}
