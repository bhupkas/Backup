/* bhupkas */

using namespace std;

#include "bits/stdc++.h"

int main()
{
	int mn;
	int t,n,w;
	cin >> t;
	while(t--)
	{
		mx = -1;
		cin >> n;
		for(int i = 0 ; i < n ; ++i)	mx = max(mx.i+w);  
	}
	return 0;
}
