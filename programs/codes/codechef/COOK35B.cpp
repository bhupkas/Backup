/* bhupkas */

#include "iostream"
#include "stdio.h"
#include "vector"
#include "map"
#include "set"
#include "algorithm"
#include "string.h"
#include "string"
#include "math.h"

using namespace std;

void pre()
	{

	}

#define PB 	push_back
#define I   insert
#define LL	long long 
#define so(v)	sort(v.begin(),v.end())
#define FOR(i,a,b)	for(i=a;i<b;i++)
#define MOD (LL)(1e9 + 7) 
#define PP pair<LL,LL>
#define MP make_pair

int main()
	{
		LL ti,j,ans,si;
		scanf("%lld",&t);
		vector<LL> v;
		char S[1000050];
		while(t--)
			{
				v.clear();
				getchar();
				scanf("%s",S);
				si=strlen(S);
				j=0;
				FOR(i,0,si)
					{
						if(S[i]=='#')	continue;
						
					}
			}
		return 0;
	}