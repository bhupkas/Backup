/* bhupkas */

#include "bits/stdc++.h"

using namespace std;

int A[1005];

int main()
{
	srand(time(NULL));
	for(int i = 0 ; i < 250000 ; ++i)	cout << (char)(rand() % 26 + 'a');
	cout << endl;
	for(int i = 0 ; i < 250000 ; ++i)	cout << (char)(rand() % 26 + 'a');
	cout << endl;
	return 0;
}
