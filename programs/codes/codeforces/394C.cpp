/* bhupkas */

#include "bits/stdc++.h"

using namespace std;

int main()
{
	int n;
	string str;
	/*
	cin >> n;
	vector < int > v(n);
	for(int i = 0 ; i < n ; i++)	cin >> v[i];
	sort(v.begin(),v.end());
	*/
	return 0;
}
