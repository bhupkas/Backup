/* bhupkas */

#include "bits/stdc++.h"

using namespace std;

int main()
{	
	int n,k;
	cin >> n >> k;
	vector < int > p(n),e(n);
	for(int i = 0 ; i < n ; i++)	cin >> p[i] >> e[i];
	vector < int > temp(n);
	int re = 1000000000;
	
	if(re == 1000000000)	cout << -1 << endl;
	else			cout << re << endl;
	return 0;
}
