/* bhupkas */

#include "bits/stdc++.h"

using namespace std;

int main()
{
	string str;
	cin >> str;
	int n = str.size();
	int re = 1;
	int last;
	for(int i = 0 ; i < n ; i++)
	{
		last = i;
		int j = i + 1;
		while(j < n)
		{	
			
		}
	}
	return 0;
}
