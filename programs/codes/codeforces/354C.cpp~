/* bhupkas */

#include "bits/stdc++.h"

using namespace std;

int n,k;
int A[300006];

int main()
{
	ios_base::sync_with_stdio(0);
	cin >> n >> k;
	for(int i = 0 ; i < n ; i++)	cin >> A[i];
	int low = 1 ;
	int  high = k;
	int mid;
	while(low <= high)
	{
		mid = (low + high) / 2;
	}
	return 0;
}
