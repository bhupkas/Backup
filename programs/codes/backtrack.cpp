#include "bits/stdc++.h"

using namespace std;

int main()
{
	int t;
	cin >> t;
	vector < int > v;
	while(t--)
	{
		int n;
		cin >> n;
		int temp;
		for(int i = 0 ; i < n  ; i++)	cin >> temp , v.push_back(temp);
		
	}
	return 0;
}
